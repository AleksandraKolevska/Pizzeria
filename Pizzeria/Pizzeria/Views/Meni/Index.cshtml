
@{
    ViewBag.Title = "Index";
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div class="row">
        @using (Html.BeginForm("SaveRecord", "Meni", FormMethod.Post))
    {

        <select id="opis" name="opis">
            @foreach (var c in ViewData.Model)
        {
            <option value="@c.TipID">@c.Tip.ImePica</option>

    }
        </select>
        <select id="opis" name="opis">
            @foreach (var c in ViewData.Model)
        {
            <option value="@c.OpisID">@c.Opi.Sostojki</option>

    }
        </select>
        <select id="opis" name="opis">
            @foreach (var c in ViewData.Model)
        {
            <option value="@c.VelicinaID">@c.Velicina.Golemina</option>

    }
        </select>
        <input type="submit" value="submit" class="btn btn-block btn-primary" />
}


    </div>
</body>
</html>
